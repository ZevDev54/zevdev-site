
// listReactFiles(__dirname).then(files => console.log(files))

export default function DrawImages() {
    return (
        <div>
            {
            
            /* {
                listReactFiles('../images/gamethumbnails').then (files => {

                    files.forEach(file => {
                        return(
                            <img src={"../images/gamethumbnails/"+file}></img>
                        )
                    });

                })
            } */}
        </div>
    )

}